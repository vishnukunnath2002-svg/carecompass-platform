You are a senior software architect and backend engineer.

I have an existing full-stack project generated using Lovable. The current architecture is:

Frontend:
React / Vite application generated by Lovable.

Backend:
Supabase (PostgreSQL, Supabase Auth, Supabase Storage, Supabase Realtime).

Goal:
Completely replace the Supabase backend with a custom backend built using Python Django and Django REST Framework (DRF) while preserving all existing frontend functionality and UI components.

Requirements:

1. DO NOT modify the frontend UI or break existing pages/components.
2. Replace all Supabase database queries with REST API calls to the new Django backend.
3. Maintain the same data structure so the frontend continues to work.
4. Implement JWT authentication instead of Supabase Auth.
5. Implement PostgreSQL database using Django ORM.
6. Support file uploads using Django storage.
7. Maintain all existing features such as:
   - user authentication
   - data fetching
   - CRUD operations
   - file uploads
   - realtime-like updates (use polling or WebSockets if needed)

---------------------------------------

Architecture Requirements

The backend must be designed as a scalable SaaS multi-tenant platform.

Use the following architecture:

Backend Stack
- Django
- Django REST Framework
- PostgreSQL
- django-tenants (schema based multi-tenancy)
- JWT Authentication (SimpleJWT)
- Redis + Celery (for background jobs)

---------------------------------------

Multi-Tenant Requirements

The platform must support multiple agencies (tenants).

Each tenant represents a separate homecare agency (like Cylo).

Use schema-based multi-tenancy with django-tenants:

Public Schema
- tenants
- domains
- subscription plans
- feature modules
- super admin management

Tenant Schema
- users
- caregivers
- services
- bookings
- patients
- orders
- notifications

Each tenant must have isolated data in its own schema.

---------------------------------------

Super Admin Features

Implement a super admin panel that can:

1. Create new tenants
2. Assign domains or subdomains
3. Enable/disable modules for tenants
4. Manage subscription plans
5. Monitor tenant activity
6. Suspend or activate tenants

---------------------------------------

Feature Modularization

The system must support configurable modules such as:

- caregiver booking
- product marketplace
- pharmacy ordering
- analytics dashboard
- caregiver scheduling
- inventory management

Modules must be enabled or disabled per tenant based on subscription plan.

Do NOT hardcode features.

Use a feature flag or module configuration system.

Example tables:

features
plans
plan_features
tenant_features

---------------------------------------

Frontend Integration

Replace all Supabase calls such as:

supabase.from("table").select()

with API calls like:

GET /api/table/

Create an API service layer in the frontend:

src/services/api.ts

Example services:
authService
bookingService
caregiverService
tenantService

The UI must remain unchanged.

---------------------------------------

Authentication

Replace Supabase Auth with JWT authentication:

Endpoints:
POST /api/auth/login
POST /api/auth/register
POST /api/auth/refresh

Roles:

super_admin
tenant_admin
staff
customer

---------------------------------------

Folder Structure

backend/
    core/
    tenants/
    users/
    caregivers/
    bookings/
    services/
    marketplace/
    notifications/

---------------------------------------

API Design

Use RESTful endpoints.

Example:

/api/auth/
/api/caregivers/
/api/bookings/
/api/services/
/api/orders/

---------------------------------------

Migration Requirements

Step 1:
Analyze the frontend code and identify all Supabase usages.

Step 2:
Map each Supabase table to a Django model.

Step 3:
Create serializers and API views for each model.

Step 4:
Replace Supabase queries in the frontend with API service calls.

Step 5:
Ensure all features work exactly as before.

---------------------------------------

Output Required

1. Django project structure
2. Django models corresponding to Supabase tables
3. API endpoints
4. Multi-tenant configuration using django-tenants
5. Super admin tenant management system
6. Frontend API integration strategy
7. Step-by-step migration plan

Ensure the final architecture is production ready and scalable for a SaaS platform serving hundreds of homecare agencies.